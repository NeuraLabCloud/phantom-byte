"use client";

import React from 'react';
import { Button, Center, Text, Title } from "@mantine/core";

export default function Projects() {
  //   const auth = useAuth()?.isAuthenticated;
  return (
    <main className="flex h-[100vh] w-[100vw] p-4">
      <p>projects list (not implemented yet). Go to <a className='text-primary' href="/projects/test">/projects/test</a></p>
    </main>
  );
}
