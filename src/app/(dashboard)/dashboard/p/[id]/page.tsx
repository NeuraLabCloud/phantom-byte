import React, { FC } from 'react';

interface ProjectViewProps {}

const ProjectView: FC<ProjectViewProps> = async ({}) => {
	return (
		<>
			<div>page</div>
		</>
	);
};

export default ProjectView;
