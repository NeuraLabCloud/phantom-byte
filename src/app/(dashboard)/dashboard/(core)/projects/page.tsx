import Cards from "@/components/ui/Cards";
import React, { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = async ({}) => {

  return (
    <>
      woprld
    </>
  );
};

export default page;
