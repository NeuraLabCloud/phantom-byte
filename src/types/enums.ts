/**
 * ? Limits on project for users.
 *
 * In the future, depending on the users plan, we might add different limits.
 */
export enum ProjectLimitations {
  MAX_PROJECTS = 3,
  MAX_PROJECT_MEMBERS = 5,
}
